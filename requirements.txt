# Developed on Python 3.13; CI runs the notebook on Python 3.11, 3.12 and 3.13.
# Install with: python -m pip install -r requirements.txt

# Lower bounds are minimums known to work. Upper bounds are only present where a
# newer release is known to break this notebook:
#   matplotlib < 3.11  -> 3.11 removed the `labels` argument of Axes.boxplot(),
#                         which section 2.7 of the notebook uses.
#   pandas < 4         -> pandas 4 will stop selecting text columns via
#                         select_dtypes(include=['object']), which the one-hot
#                         encoding in section 3 relies on.

numpy>=1.26
pandas>=2.1,<4
matplotlib>=3.8,<3.11
scipy>=1.11
scikit-learn>=1.4
joblib>=1.3
threadpoolctl>=3.1

# Only needed to open or execute the notebook, not to read it on GitHub.
jupyterlab>=4.0
ipykernel>=6.25
nbconvert>=7.0
